# ============================================================================
# DRK
# ============================================================================

# --- dependencies / build ---------------------------------------------------
node_modules/
.next/
.next-prod/
out/
build/
*.tsbuildinfo
next-env.d.ts
.env*

# --- QA artefacts -----------------------------------------------------------
# Screenshot sets are regenerated by `node scripts/audit-shots.mjs`.
.review/
lh.json
*.log

# --- OS ---------------------------------------------------------------------
.DS_Store
Thumbs.db

# ============================================================================
# CONFIDENTIAL SOURCE MATERIAL — deliberately not published
# ============================================================================
# These are the client's own materials and are excluded by default because a
# repository's visibility cannot be assumed:
#
#   Pitch deck.pdf   marked "DRK / CONFIDENTIAL" on every page
#   demo.mp4         a screen recording of the live DRK application on mainnet,
#                    including real token, pool and wallet values
#
# Everything the build needs is already committed — the transcribed content
# lives in `interactive-deck/content/drk.ts` and the derived object plates in
# `interactive-deck/public/brand/objects/`. Remove these two lines if the
# repository is private and the originals should travel with it.
Pitch deck.pdf
demo.mp4
